// ============================================================
// DD-07 실시간(RT) opt-in 서브패스 엔트리 — `open-grid/realtime`. / DD-07 opt-in subpath entry.
// ------------------------------------------------------------
// 코어 진입점(src/index.ts)은 이 파일을 참조하지 않는다 → RT 미사용 시 코어 gzip 증분 0
// (REQ-RT-801 수용기준). 전송/타이머/rAF 는 전부 주입(헤드리스·서버 안전).
// 통합 TODO: package.json exports 에 "./realtime" 서브패스 등록(병렬 개발 머지 충돌 회피 위해 통합 단계에서).
// ============================================================

export * from '../core/realtime/index.js';
