/**
 * OPEN_GRID React 바인딩 진입점(`open-grid/react`). / OPEN_GRID React binding entry point (`open-grid/react`).
 *
 * @packageDocumentation
 */

// 컴포넌트 재수출 — 상세 문서는 선언 원본(OpenGrid.tsx)에 있다. / Component re-export — detailed docs live on the declaration (OpenGrid.tsx).
export { OpenGridReact as OpenGrid } from './OpenGrid.js';
// props 타입 재수출 / props type re-export
export type { OpenGridProps } from './OpenGrid.js';
